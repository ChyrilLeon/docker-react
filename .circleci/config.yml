version: 2.1

workflows:
  version: 2
  build:
    jobs:
      - tests

jobs:
  tests:
    docker:
      - image: leonchyril/react-for-tests
    steps:
      - checkout
      - run:
          name: tests
          command: npm test
